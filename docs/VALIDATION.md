# 公开整理验证记录

验证日期：2026-09-24。

本次工作整理和发布已有研究材料，没有重新训练模型或重新得出物理结论。

已执行：

- 对公开 Python 源文件进行语法检查。
- 运行 `scripts/show_results.py`，核对其输出来自保存的 v21 bootstrap JSON。
- 检查新建项目导航文档的本地链接。
- 检查选入的文本、报告及文件类型，排除原始数据、逐事件输出、内部服务器管理工具、嵌套压缩包和二进制模型；将文本中的本地用户路径改为示例值。

完整性校验由 `scripts/verify_snapshot.py` 与 `docs/PUBLIC_MANIFEST.json` 提供。校验只能验证公开文件是否与此快照一致，不能证明科学结论正确。

未执行：完整重跑所有历史训练、对新 run 的独立盲测、所有旧报告的重新排版编译。隔离环境的依赖安装耗时过长，已取消，因此本次没有完成 v21 依赖导入和命令入口运行检查；不能将语法检查视为端到端运行验证。最新阶段仍是候选方案，详见项目首页的限制说明。
